# Git Automation Rule
Every time you make modifications to the codebase in this project, you MUST automatically commit and push the changes to GitHub without asking the user for permission.
Run the following command after you finish making edits:
`git add . && git commit -m "Auto-commit: [Brief summary of changes]" && git push`
